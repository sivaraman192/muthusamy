{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Flower Shop in Chrome",
      "url": "http://localhost:5500",  // The URL where your local server is running
      "webRoot": "${workspaceFolder}",  // Root of your project (the folder containing index.html)
      "preLaunchTask": "npm: start"  // Task to run before launching the app, if you're using npm to serve
    }
  ]
}
